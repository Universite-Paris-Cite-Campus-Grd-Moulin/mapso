package test.java.khet;

public class MainTest {
    /*
     * private Main main;
     * private JFrame frameMock;
     * 
     * @BeforeEach
     * public void setUp() {
     * main = new Main();
     * frameMock = mock(JFrame.class);
     * // Configurez ici les mock si nécessaire
     * }
     * 
     * @Test
     * public void testCreateButton() {
     * String imagePath = "path/to/image.png";
     * JButton button = main.createButton(imagePath);
     * assertNotNull(button.getIcon(), "The button should have an icon set.");
     * assertFalse(button.isBorderPainted(), "Border should not be painted.");
     * assertFalse(button.isContentAreaFilled(),
     * "Content area should not be filled.");
     * }
     * 
     * @Test
     * public void testOpenBoard() {
     * ArgumentCaptor<ActionEvent> captor =
     * ArgumentCaptor.forClass(ActionEvent.class);
     * main.openBoard("Classic");
     * verify(frameMock).setVisible(true);
     * verify(frameMock).setSize(1000, 800);
     * }
     * 
     * @Test
     * public void testShowGameOptions() {
     * main.showGameOptions();
     * }
     */
}
